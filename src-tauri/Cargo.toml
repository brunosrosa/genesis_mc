[package]
name = "souls_mc"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

[workspace]
members = ["crates/anthropophagy"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "souls_mc_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "=2.6.3", features = [] }

[dependencies]
tauri = { version = "=2.11.5", features = ["tray-icon"], optional = true }
tauri-plugin-opener = { version = "=2.5.4", optional = true }
serde = { version = "=1.0.228", features = ["derive"] }
serde_json = "=1.0.149"  # SOULS-CANIBALIZED Fase 2.5: alinhar com pin do lean-ctx
httparse = "=1.10.1"
jsonwebtoken = { version = "=10.4.0", default-features = false, features = ["use_pem", "rust_crypto"] }
regex = "=1.12.4"
pulldown-cmark = "=0.13.4"
jsonrepair = "=0.1.0"  # SOULS-CANIBALIZED Marco 4.9.2: parser recursivo estrutural para cura de JSON malformado (sem `.replace` cego que corromperia strings literais; ADR-030 Módulo 4 pin rígido)
# SOULS-CANIBALIZED Fase 3: lean-ctx EXPURO do Cargo.toml.
# Canibalização Tipo A: extraímos a "Alma Matemática" (dot-flattening, ANSI filter,
# Myers diff) como código NATIVO em src-tauri/src/cognition/lean_vacuum/.
# A pasta third_party/lean-ctx/ permanece como cadáver READ-ONLY apenas para consulta.
# (Não usar `path = "third_party/lean-ctx"` — SOULS dita as regras.)
tinyrand = "=0.5.0"
tokio = { version = "=1.51.1", features = ["full"] }  # SOULS-CANIBALIZED Fase 2.5: downgrade para pin do lean-ctx (1.52.3 quebrou path dep)
sysinfo = "=0.30.13"
thiserror = { version = "=2.0.19", default-features = false }  # SOULS-CANIBALIZED Marco 4.2.2: pin rígido v2.0.19 (unifica thiserror v2 no workspace; evita regressões de std/no_std)
ignore = "=0.4.25"  # SOULS-CANIBALIZED Fase 2.5: alinhar com pin do lean-ctx
dunce = "=1.0.5"
tracing = "=0.1.44"
url = "=2.5.8"
sha2 = "=0.10.9"
toml = "=1.1.4"  # SOULS-CANIBALIZED Marco 4.2.2: pin rígido v1.1.4 (alinhado com spec 1.1.0 e parsers toml_writer/toml_parser)
tiktoken = "=3.5.1"
oxc = { version = "=0.120.0", features = ["ast_visit"] }
astral-tl = "=0.7.11"
gix = { version = "=0.84.0", features = ["blocking-network-client", "worktree-mutation"] }
quick-xml = "=0.39.4"
reqwest = { version = "=0.12.28", default-features = false, features = ["json", "blocking", "rustls-tls-webpki-roots", "charset", "http2"] }
rusqlite = { version = "=0.39.0", features = ["bundled"] }  # SOULS-CANIBALIZED Fase 2.5: downgrade 0.40.1→0.39.0 (libsqlite3-sys 0.37.0, sem cfg_select! unstable em rustc 1.94.1)
lancedb = { version = "0.30.0", default-features = false }
arrow-array = "=58.4.0"
arrow-schema = "=58.4.0"
arrow-row = "=58.4.0"
arrow-cast = "=58.4.0"

futures-util = "=0.3.31"
sqlparser = "=0.62.0"

tracing-subscriber = { version = "=0.3.23", features = ["env-filter"] }
tempfile = "=3.27.0"
dotenvy = "=0.15.7"
quote = "=1.0.46"
tree-sitter = "=0.26.10"
tree-sitter-c-sharp = "=0.23.5"
tree-sitter-yaml = "=0.7.2"
bumpalo = { version = "=3.20.3", features = ["collections"] }
memmap2 = "=0.9.11"
# html-to-markdown-rs removido — extração de texto via astral-tl zero-copy
enable-ansi-support = "=0.2.1"
uuid = { version = "=1.23.4", features = ["v4"] }
windows-sys = { version = "=0.61.2", features = [  # SOULS-CANIBALIZED Marco 4.2.2: unificação rígida v0.61.2 (alinhado com tokio 1.51, gix, tempfile e which; erradica duplicação em RAM - ADR-030)
    "Win32_Foundation",
    "Win32_Security",
    "Win32_Security_Authorization",
    "Win32_Security_Isolation",
    "Win32_Storage_FileSystem",
    "Win32_System_IO",
    "Win32_System_JobObjects",
    "Win32_System_Pipes",
    "Win32_System_Threading",
    "Win32_System_StationsAndDesktops",
    "Win32_UI_Input_KeyboardAndMouse",
    "Win32_System_SystemInformation",
] }
nvml-wrapper = "0.10.0"
rustc-hash = "=2.1.3"
which = "=6.0.3"
zstd = "=0.13.3"
walkdir = "=2.5.0"
dashmap = "6.1.0"
similar = "=2.7.0"  # SOULS-CANIBALIZED Fase 3: Myers diff nativo (homologada; antes vinha como dep transitiva do lean-ctx)
wasmtime = { version = "=29.0.0", features = ["wat"] }
wasmtime-wasi = "=29.0.0"

llama-cpp-2 = { version = "0.1", optional = true, features = ["cuda"] }
mistralrs = { version = "0.8", optional = true }
llguidance = { version = "1.7", optional = true }

[target.'cfg(windows)'.dependencies]
windows-projfs = "=0.1.8"

[patch.crates-io]
crc32fast = { path = "vendor/crc32fast" }
llama-cpp-2 = { path = "vendor/llama-cpp-2" }
llama-cpp-sys-2 = { path = "vendor/llama-cpp-sys-2" }

[dev-dependencies]
mockito = "=1.7.2"
tempfile = "=3.27.0"

[features]
default = []
tauri-app = ["dep:tauri", "dep:tauri-plugin-opener"]
llama_backend = ["dep:llama-cpp-2", "llama-cpp-2/cuda", "dep:llguidance"]
mistral_backend = ["dep:mistralrs"]
grammar_backend = ["dep:llguidance"]
# Marco 4.10.1 — ETAPA 5: gateway_ccr é um marker feature que gateia o
# binário `agentgateway_tcp_proxy` (o único que depende do compressor
# CCR + L7 Shield). A lib (souls_mc_lib) mantém a CCR API sempre
# disponível, mas o proxy só compila com a flag ON.
gateway_ccr = []

[[bin]]
name = "souls_mc"
path = "src/main.rs"
required-features = ["tauri-app"]

[[bin]]
name = "souls_mcp_server"
path = "src/bin/souls_mcp_server.rs"

[[bin]]
name = "souls_vanguard_worker"
path = "src/bin/souls_vanguard_worker.rs"
required-features = ["llama_backend"]

# Marco 4.10.1 — ETAPA 5: binários CCR-gated. O proxy L7 + o stdio guard
# só compilam com a feature `gateway_ccr` (não inflando binários de
# produção que não precisam do compressor).
[[bin]]
name = "agentgateway_tcp_proxy"
path = "src/bin/agentgateway_tcp_proxy.rs"
required-features = ["gateway_ccr"]

[[bin]]
name = "mcp_stdio_guard"
path = "src/bin/mcp_stdio_guard.rs"
required-features = ["gateway_ccr"]

# ---------- PROFILES (fix/cargo-finops-v1) ----------
# Dev/test já estão definidos abaixo. Aqui entramos com [profile.release]:
# lto=thin (cross-module sem dobar tempo de link), panic=abort (binário menor,
# sem unwind), codegen-units=1 (LTO eficaz), strip=symbols (release sem PDB).
# Opt-level=3 porque temos inferência numérica (mistralrs/llama) que se beneficia
# agressivamente de AVX2 — não trocamos runtime por tamanho.
# ATENÇÃO: settings granulares de package.* vivem em .cargo/config.toml (workspace-level).
[profile.dev]
incremental = false

[profile.test]
incremental = false

[profile.release]
lto = "thin"
panic = "abort"
codegen-units = 1
strip = "symbols"
opt-level = 3
incremental = false

