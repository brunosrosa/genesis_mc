=== SODA MCPs LIST (ZERO-BRAND CANONICAL) ===
GERADO EM: 2026-07-26 18:45:20
TOTAL DE FERRAMENTAS: 58
----------------------------------------------------------------------------------------------------
• core_analyze_session: Analyze the current thinking session and provide insights
• core_export_session: Export the current thinking session in various formats
• core_merge_sessions: Merge multiple thinking sessions into one
• core_think: Dynamic CoT tool. Break down complex problems, branch, backtrack, and verify hypotheses iteratively.
• mem_add_observations: Add new observations to existing entities in the knowledge graph.
• mem_create_entities: Create multiple new entities with their types and observations in the knowledge graph.
• mem_create_relations: Create multiple new directed relations between existing entities in the knowledge graph.
• mem_graph: Retrieve the complete knowledge graph structure containing all entities and relations.
• mem_open_nodes: Retrieve specific entities by name with their observations and relations.
• mem_search: Search the knowledge graph using full-text search over node names, types, and observations.
• soda_db_query: Executa consulta SQLite local em modo somente leitura nos bancos nativos do SODA.
• soda_repo_ast: Extrai o blueprint AST do repositório usando o parser nativo em Rust.
• soda_repo_meta: Extrai metadados GitHub nativos via octocrab para owner/repo.
• soda_sys_time: Retorna data/hora local, UTC e fuso atual via chrono nativo.
• soda_web_fetch: Busca uma URL com Tentativa Dupla nativa do SODA e retorna markdown limpo.
• soda_web_search: Executa busca web nativa contra DuckDuckGo HTML e retorna titulos, links e snippets.
• souls_agent: Multi-agent coordination (shared message bus + persistent diaries). Actions: register (join with agent_type+role), post (broadcast or direct message with category), read (poll messages), status (update state: active|idle|finished), handoff (transfer task to another agent with summary), sync (overview of all agents + pending messages + shared contexts), diary (log discovery/decision/blocker/progress/insight — persisted across sessions), recall_diary (read agent diary), diaries (list all agent diaries), list, info.
• souls_architecture: Graph-based architecture analysis. Actions: overview|clusters|layers|cycles|entrypoints|module.
• souls_callees: Find all functions/methods called by a given symbol. Returns callee name, file, and line.
• souls_callers: Find all symbols that call a given function/method. Returns caller file, symbol, and line.
• souls_compress: Checkpoint context to compress conversation history.
• souls_compress_memory: Compress a memory/config file (CLAUDE.md, .cursorrules, etc.) to save tokens on every session start. Preserves code blocks, URLs, paths, headings, tables. Creates .original.md backup.
• souls_context: Session context overview — cached files, seen files, session state.
• souls_cost: Cost attribution (local-first). Actions: report|agent|tools|json|reset.
• souls_dedup: Cross-file dedup: analyze or apply shared block references.
• souls_delta: Incremental diff — sends only changed lines since last read.
• souls_edit: Edit a file via search-and-replace. Works without native Read/Edit tools. Use this when the IDE's Edit tool requires Read but Read is unavailable.
• souls_execute: Run code in sandbox (11 languages). Only stdout enters context. Raw data never leaves subprocess. Languages: javascript, typescript, python, shell, ruby, go, rust, php, perl, r, elixir.
• souls_feedback: Harness feedback for LLM output tokens/latency (local-first). Actions: record|report|json|reset|status.
• souls_fill: Budget-aware context fill — auto-selects compression per file within token limit.
• souls_gain: Gain report.
• souls_graph: Code dependency graph. Actions: build (index project), related (find files connected to path), symbol (lookup definition/usages as file::name), impact (blast radius of changes to path), status (index stats).
• souls_graph_diagram: Generate a Mermaid diagram of the dependency or call graph. Useful for understanding architecture.
• souls_handoff: Context Ledger Protocol (hashed, deterministic, local-first). Actions: create|show|list|pull|clear.
• souls_heatmap: File access heatmap (local-first). Actions: status|directory|cold|json.
• souls_impact: Graph-based impact analysis. Actions: analyze|chain|build|status.
• souls_intent: Structured intent input (optional) — submit compact JSON or short text; server also infers intents automatically from tool calls.
• souls_knowledge: Persistent project knowledge (survives sessions). Actions: remember (store fact with temporal tracking + contradiction detection), recall (search), pattern (record convention), consolidate (extract session findings), gotcha (record a bug/mistake to never repeat — trigger+resolution required), timeline (view fact history for a category), rooms (list knowledge categories), search (cross-session search across ALL projects), wakeup (compact AAAK briefing), status (list all), remove, export, embeddings_status|embeddings_reset|embeddings_reindex (local semantic index management for recall).
• souls_metrics: Show token usage metrics and cache efficiency stats.
• souls_multi_read: Batch read files. Same modes as ctx_read.
• souls_outline: List all symbols in a file (functions, structs, classes, methods) with signatures. Much fewer tokens than reading the full file.
• souls_overview: Task-relevant project map — use at session start.
• souls_prefetch: Predictive prefetch — prewarm cache for blast radius files (graph + task signals) within budgets.
• souls_preload: Proactive context loader — caches task-relevant files, returns L-curve-optimized summary (~50-100 tokens vs ~5000 for individual reads).
• souls_read: Read file (cached, compressed). Auto-selects optimal mode. Modes: full, map, signatures, diff, lines:N-M. fresh=true forces re-read.
• souls_response: Compress LLM response text (remove filler, apply TDD).
• souls_routes: List HTTP routes/endpoints extracted from the project. Supports Express, Flask, FastAPI, Actix, Spring, Rails, Next.js.
• souls_search: Search files using regular expressions. Respects .gitignore patterns.
• souls_semantic_search: Semantic code search (BM25 + optional embeddings/hybrid). action=reindex to rebuild.
• souls_session: Cross-session memory (CCP). Actions: load (restore previous session ~400 tok), save, status, task (set current task), finding (record discovery), decision (record choice), reset, list (show sessions), cleanup.
• souls_share: Share cached file contexts between agents. Actions: push (share files from your cache to another agent), pull (receive files shared by other agents), list (show all shared contexts), clear (remove your shared contexts).
• souls_shell: Run shell command with pattern-based output compression. Use strictly for fast context compression (git/cargo). DO NOT use for long-running builds. Will be SIGKILLED after 30s.
• souls_smart_read: Auto-select optimal read mode for a file.
• souls_symbol: Read a specific symbol (function, struct, class) by name. Returns only the symbol code block instead of the entire file. 90-97% fewer tokens than full file read.
• souls_task: Multi-agent task orchestration. Actions: create|update|list|get|cancel|message|info.
• souls_tree: Directory listing with file counts. Recursively lists directory contents up to max depth.
• souls_workflow: Workflow rails (state machine + evidence). Actions: start|status|transition|complete|evidence_add|evidence_list|stop.
• souls_wrapped: Savings report card. Periods: week|month|all.
----------------------------------------------------------------------------------------------------
